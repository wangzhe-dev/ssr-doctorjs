'use client';

// Placeholder chart component that uses browser APIs
export default function ChartWidget() {
  return (
    <div style={{ padding: '1rem', border: '1px solid #ccc' }}>
      <h4>Chart Widget</h4>
      <p>This component uses browser-specific APIs for rendering charts</p>
    </div>
  );
}
