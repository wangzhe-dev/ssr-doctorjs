'use client';

// Placeholder map component that uses browser APIs
export default function MapWidget() {
  return (
    <div style={{ padding: '1rem', border: '1px solid #ccc' }}>
      <h4>Map Widget</h4>
      <p>This component uses browser geolocation and mapping APIs</p>
    </div>
  );
}
